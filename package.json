{
  "name": "truephone-api-generator",
  "version": "0.0.2",
  "description": "Typescript interface generator for the Truephone API",
  "scripts": {
    "api": "rm -rf gen/api && openapi-generator-cli generate -i res/connectapi.yml --generator-name typescript-axios -o gen/api --config api.json --skip-validate-spec && cd gen/api && npm install && npm run build"
  },
  "author": "Jorge Rodriguez",
  "license": "MIT",
  "dependencies": {
    "typescript-axios": "file:gen/api"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.1.16"
  }
}
